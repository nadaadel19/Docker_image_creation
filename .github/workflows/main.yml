on:

  push:
    branches:
      - master



jobs:
  build:
   
    run-on: ubuntu-latest 
  
    steps:
      - name: "testing github actions"
        run: pwd
      - name: "testing printing"
        run: echo ahmed